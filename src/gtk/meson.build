# gnome = import('gnome')

# gui_sources += gnome.compile_resources(
#     'wlipgtk', 'wlipgtk.gresource.xml',
# )

gui_sources += files(
  'config.c',
  'wlipgtk.c',
  'ipc.c',
  'clipboard-list.c',
  'clipboard-entry.c'
)
gui_includes += include_directories('.')
