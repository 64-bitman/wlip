sources += files('alloc.c', 'array.c', 'clipboard.c', 'database.c', 'event.c', 'hashtable.c', 'util.c', 'wayland.c')
includes += include_directories('.')

subdir('lua')
subdir('sha256')
subdir('protocol')
