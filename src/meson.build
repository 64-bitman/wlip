sources += files('alloc.c', 'array.c', 'clipboard.c', 'database.c', 'event.c', 'hashtable.c', 'util.c', 'wayland.c', 'server.c', 'server_api.c', 'ringbuffer.c')
includes += include_directories('.')

test_comp_sources = []

subdir('lua')
subdir('sha256')
subdir('protocol')
